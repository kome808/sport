> AI 注意事項：此文件為本專案的「絕對規則」，優先權高於所有其他指令。違反此文件中的禁止事項將被視為重大錯誤。
> 

## 0. 核心指令 (PRIME DIRECTIVE)

在執行任何功能開發前，必須遵守以下閱讀與確認順序：

1. **讀取背景**：閱讀 `guidelines/Product_Context.md`，理解系統核心主詞、角色與全域資料流。（此條不變）
2. **讀取全域業務規則**：閱讀 `docs/spac/rules.md`，確認本次需求是否涉及既有規則、是否會造成規則衝突。
3. **讀取規格／規劃**：閱讀 `docs/plan/[模組名稱].md`，確認當前任務的具體 UI/UX 與邏輯需求。
4. **寫程式碼前後一致性檢查（必做）**：
   - **寫程式碼前**：確認 `rules.md` 與該模組 `plan` 文件已完整描述本需求關鍵規則與流程。
   - **寫程式碼後**：對照 `rules.md` 與該模組 `plan` 文件逐條檢核，確認實作行為一致；若不一致，必須先回寫文件再修正實作。

> 文件層級優先順序（由高到低）：
> `guidelines/Product_Context.md` → `docs/spac/rules.md` → `docs/plan/[模組名稱].md` → 程式碼實作


## 1. 開發流程 (Development Workflow)

### 1.1 需求確認與規劃（先討論，後文件）

- **流程啟動：** 每次提出新功能需求時，AI 需先與使用者討論並確認細節（目標、情境、主要流程、權限、例外處理）。
- **文件化：** 確認後的需求與實作邏輯，必須由 AI 整理並寫入 `docs/plan/[模組名稱].md`。
- **規則同步：** 若本次需求新增／修改了跨模組的業務規則，必須同步更新 `docs/spac/rules.md`，並以 `rules.md` 為準。
- **執行：** 規劃文件確認無誤後，方可開始編寫程式碼。

### 1.2 模組化結構

- **隔離性：** 各模組程式碼必須分開存放，嚴禁高度耦合。
- **目標：** 更新某模組程式碼時，確保不會影響或中斷其他模組的運作。

### 1.3 全域業務規則庫：`docs/spac/rules.md`（Business Logic Rules）

- **目的：** 集中管理跨模組的業務規則（名詞定義、狀態命名、權限、資料驗證、操作限制、跨頁連動），避免規格分散與互相矛盾。
- **內容限制：** 僅寫業務規則與驗證限制，避免放入 SQL、API Endpoint、變數名稱或 CSS Class。
- **更新規則：**
  - `rules.md` 會隨系統開發持續調整。
  - AI 在 **撰寫程式碼前與完成後** 必須確認實作是否符合 `rules.md`。
  - 若本次需求會改變既有規則：必須 **先更新 `rules.md` 並與使用者確認**，再進入實作。

### 1.4 模組完成後的「功能規格書」輸出規範（System Analyst 視角）

當系統模組開發完畢，且使用者要求產生規格文件時，必須嚴格遵守以下規範與範本：

- **檔案位置**：產出之 Markdown 檔案必須存放於 `docs/spac/` 目錄下，命名格式為 `docs/spac/[模組名稱].md`。
- **角色切換**：暫時放下工程師身分，轉為「系統分析師 (System Analyst)」視角。
- **業務導向**：內容僅描述業務邏輯、驗證規則與資料流向。
- **零程式碼 (No-Code)**：嚴禁出現 SQL、API Endpoint、變數名稱或 CSS Class。
- **規則優先**：必須優先詳列「資料驗證」、「操作限制」與「權限控制」。


## 2. 核心架構與資料存取 (Architecture & Data Access)

本專案採用 **Adapter Pattern (轉接器模式)** 以支援從「本地開發」無縫遷移至「Supabase 上線」。

### 2.1 資料存取層 (Data Access Layer)

- **Adapter 介面一致性**：無論是 `LocalAdapter` 或 `SupabaseAdapter`，回傳格式必須統一模擬 Supabase 格式：`{ data: T | null, error: Error | null }`。
- **強制非同步 (Async by Default)**：本地端實作即使讀取 localStorage，也必須封裝為 Promise (Async/Await)，確保未來切換至網路請求時 UI 層無需修改。
- **Singleton Pattern**：禁止重複建立 Client。
    - **規範**：必須檢查 `StorageFactory`，確保全域僅有一個 Client 實例。

### 2.2 本地開發階段 (Local Phase)

- **儲存方式**：資料暫存於本地端 (Local Storage)。
- **Schema 模擬**：本地儲存的 Key 命名需對應未來的 `[schema]_[table]` 格式。

## 3. 嚴格禁制事項 (Strict Prohibitions & Rules)

以下規則為系統穩定性的關鍵，違反將導致 Crash 或 404 錯誤。

- **🔴 禁止 1：使用 `import.meta.env`**
    - **理由**：預覽環境無環境變數，讀取會導致 Crash。
    - **規範**：連線資訊 (URL, Key) 必須從 localStorage 讀取，或透過 UI 輸入框由使用者提供。
- **🔴 禁止 2：使用 `kv_store`**
    - **理由**：本系統採用關聯式結構，不使用 Key-Value 隨意儲存。
    - **規範**：絕對不要建立、查詢或引用任何以 `kv_store` 開頭的表格。所有資料必須存入規範好的結構化表格中。
- **🔴 禁止 3：對一般資料使用 Edge Functions**
    - **理由**：這會導致 404 錯誤，且增加延遲。
    - **規範**：讀取模組列表、專案項目等 CRUD 操作，必須使用 Direct Client 直接對資料庫操作。
    - **例外**：僅有「AI 生成/推論」相關功能需使用 Edge Functions 作為安全代理。
- **🔴 禁止 4：寫死假資料 (No Static Mocks)**
    - **理由**：阻礙真實資料流測試與 Adapter 切換。
    - **規範**：不要在 Component 裡直接寫 `const data = [...]`。資料必須透過 Adapter 從儲存端 (DB/Local) 真實讀取。
- **🔴 禁止 5：使用 `.single()` 查詢非必要資料**
    - **理由**：當查詢結果為空時，`.single()` 會回傳 406 Not Acceptable error 導致程式崩潰 (特別是讀取 system_config 時)。
    - **規範**：一律改用 `.maybeSingle()`，允許並處理查詢結果為 null 的情況。
- **🔴 禁止 6：寫死 Schema 名稱**
    - **理由**：Schema 名稱會隨專案變動。
    - **規範**：禁止在程式碼中寫死 `'myschema'` 字串。必須使用變數傳遞，例如 `supabase.schema(config.schema_name)`。

## 4. 樣式系統 (Styling System)

- **框架版本**：Tailwind CSS 4.0。
- **Design Tokens**：必須於 `/styles/globals.css` 中定義。
- **變數使用**：必須使用 CSS 變數來管理顏色、間距、圓角、陰影等，以利未來主題切換。
- **元件庫限制**：**禁止** 使用 Shadcn UI（除非專案環境中已預先安裝且規定使用），優先使用原生 HTML 搭配 Tailwind 實作。
- **字型與樣式規範**：所有生成的 UI 必須嚴格使用 `/styles/globals.css` 中定義的 CSS 變數與字型 (Font Faces)，不得自行定義未規範的樣式數值。

## 5. 部署與遷移策略 (Deployment & Migration)

當功能開發完成準備上線時，執行以下標準作業程序 (SOP)：

1. **指定 Schema**：由使用者指定正式的 Schema 名稱。
2. **產出 SQL**：AI 產出完整的 DDL (建表) 與 DML (RLS 權限設定) SQL 指令。
3. **手動執行**：使用者至 Supabase Dashboard 手動執行 SQL。
4. **切換 Adapter**：使用者通知 AI 「SQL 已執行」。
5. **AI 修改程式碼**，將 `StorageFactory` 的實例化對象從 `LocalAdapter` 切換為 `SupabaseAdapter`。UI 層程式碼保持不變。

##6 模組規格書撰寫規範 (Module Specification Standards)
當需要為模組撰寫「功能規格書」時，必須嚴格遵守以下範本結構與規則。

6.1 撰寫原則

- **檔案位置**：產出之 Markdown 檔案必須存放於 `docs/` 目錄下，命名格式為 `docs/[模組名稱]_規格書.md`。
- **角色切換**：暫時放下工程師身分，轉為「系統分析師 (System Analyst)」視角。
- **業務導向**：內容僅描述業務邏輯、驗證規則與資料流向。
- **零程式碼 (No-Code)**：嚴禁出現 SQL、API Endpoint、變數名稱或 CSS Class。
- **規則優先**：必須優先詳列「資料驗證」、「操作限制」與「權限控制」。

6.2 規格書標準範本 (Template)
產出文件時，請依照下列 Markdown 結構填寫：

---

# [模組名稱] 功能規格書

> 文件版號：V1.0
產出日期：{YYYY-MM-DD}
> 

## 1. 模組概述 (Overview)

- **模組目的**：(用一句話說明這個模組是用來解決什麼問題的)
- **目標使用者**：(例如：一般玩家、系統管理員)
- **核心價值**：(使用此模組能帶來什麼效益？)

## 2. 功能清單 (Feature List)

- **[查詢]**：(User Story...)
- **[新增]**：(User Story...)
- **[操作]**：(User Story...)

## 3. 重要規則與限制 (Core Business Rules) 🔥

*在此章節集中定義所有「不可違反」的硬性規則，開發時需優先檢查此處。*

### 3.1 資料驗證 (Data Validation)

- **格式檢查**：(例如：Email 格式、電話僅限數字)
- **範圍限制**：(例如：數值 1-100、金額 > 0)
- **唯一性檢查**：(例如：名稱不可重複)
- **關聯驗證**：(例如：結束日期 > 開始日期)

### 3.2 操作限制 (Operation Constraints)

- **狀態互斥**：(例如：狀態為「已結案」禁止修改)
- **前置條件**：(例如：需先選擇分類才能新增項目)
- **刪除保護**：(例如：有關聯資料禁止刪除)
- **數量限制**：(例如：上限 10 筆)

### 3.3 顯示規則 (Display Rules)

- **數值格式**：(例如：千分位逗號、小數點位數)
- **狀態樣式**：(例如：異常顯示紅字)
- **預設排序**：(例如：依建立時間由新到舊)

### 3.4 權限控制 (Permission / Access Control)

- **角色權限**：(例如：僅管理員可見)
- **功能解鎖**：(例如：單機版需滿足特定條件才開放)

## 4. 介面互動與流程 (UI Interaction Flow)

### 4.1 [畫面名稱]

- **畫面構成**：(簡述區塊，引用 3.3 規則)
- **使用者操作**：點擊 [按鈕] 後...
- **系統回饋**：
    - 若符合 [3.1] 與 [3.2] -> 執行成功...
    - 若違反規則 -> 顯示錯誤訊息...

## 5. 資料欄位定義 (Data Dictionary - Business View)

> 格式：| 欄位名稱 | 欄位含義 | 資料格式範例 | 備註/限制 |
請以業務觀點撰寫，勿寫 Database Type。
> 

## 6. 資料流向 (Data Flow)

1. **資料從哪裡來 (Input)**：(例如：使用者輸入、系統自動帶入)
2. **如何處理 (Process)**：(例如：檢查庫存、計算稅額)
3. **儲存到哪裡 (Output)**：(例如：儲存新紀錄、更新暫存)

## 7. 測試案例 (Test Cases)

> 格式：| ID | 測試場景 | 操作步驟 | 預期結果 |
需包含 TC-01 [成功]、TC-02 [失敗/驗證錯誤]、TC-03 [邏輯/操作限制] 等情境。
>
